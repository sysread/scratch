Structure these notes into a JSON object for a conversation search index. Two fields:

1. `summary`: 2-4 terse sentences. Name specific topics, files, and decisions. No filler.

2. `questions`: 5-7 short queries someone would type into a search bar to find this conversation. Be specific to what was discussed, not generic.

Respond with ONLY the JSON object.

Notes:

{{notes}}