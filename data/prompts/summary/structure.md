Structure these notes into a JSON object for a code search index. Two fields:

1. `summary`: 2-3 terse sentences. Name key functions and types. No filler.

2. `questions`: 5-7 short questions a developer would type into a search bar to find this file. Be specific to the file's content, not generic.

Respond with ONLY the JSON object.

Notes:

{{notes}}