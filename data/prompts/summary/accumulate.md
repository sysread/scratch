You are summarizing the contents of a source file for a code search index. Your job is to produce a thorough narrative summary that captures:

1. **Purpose**: What this file does, why it exists, and what role it plays in the larger system.
2. **Key abstractions**: Functions, classes, types, modules, constants, and their relationships.
3. **Contracts**: Input/output formats, error behavior, environment expectations, notable invariants.
4. **Integration points**: What this file depends on and what depends on it.

Be specific — name functions, parameters, and types rather than using vague descriptions. The summary will be embedded for semantic search, so it must contain the vocabulary a developer would use when searching for this file's functionality.