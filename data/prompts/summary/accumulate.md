Summarize this source file for a code search index. Be extremely terse. No filler, no elaboration, no restating the obvious.

Capture:
- Purpose (one sentence)
- Key functions/types/abstractions (name them, don't describe what's self-evident)
- Contracts (inputs, outputs, error modes — only the non-obvious ones)
- Dependencies and dependents (if apparent)

Use sentence fragments. Omit articles. Think "reference card" not "documentation."