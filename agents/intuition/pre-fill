#!/usr/bin/env bash

#-------------------------------------------------------------------------------
# intuition pre-fill
#
# Passthrough. The intuition agent manages its own prompts across
# multiple phases (perception, drive reactions, synthesis) and does not
# use a single system prompt. Messages pass through unchanged.
#-------------------------------------------------------------------------------

set -euo pipefail

cat
