#!/usr/bin/env bash

#-------------------------------------------------------------------------------
# intuition agent - availability check + dependency manifest
#
# The intuition agent uses workers + chat + model + prompt. Declare every
# leaf binary that flows through those layers so the doctor reports them
# under agent:intuition. chat and model use jq and curl through venice:curl;
# venice:curl uses bc for retry backoff math.
#-------------------------------------------------------------------------------

set -euo pipefail

# shellcheck source=/dev/null
source "$SCRATCH_HOME/lib/base.sh"

has-commands curl jq bc
