{
  "name": "coordinator",
  "description": "General-purpose conversational assistant for interactive chat sessions. Receives the full conversation history on stdin as a JSON messages array, prepends a system prompt, and returns the assistant's response."
}
